schema: http://localhost:4000/graphql
documents: ./graphql/**/*.ts
overwrite: true
generates:
  ./types/apollo-components.types.ts:
    config:
      noNamespaces: true
    plugins:
      - typescript-common:
      - typescript-client:
      - typescript-react-apollo:
