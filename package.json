{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@example/common,@example/server}",
    "build:web": "lerna run build --scope={@example/common,@example/web}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "3.6.0"
  },
  "resolutions": {
    "graphql": "14.0.2"
  },
  "dependencies": {
    "rimraf": "^2.6.2"
  }
}
